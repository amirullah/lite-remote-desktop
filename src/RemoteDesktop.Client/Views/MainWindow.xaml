<Window x:Class="RemoteDesktop.Client.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LiteRemote" Height="760" Width="1120"
        Icon="/Assets/literemote.ico"
        Background="{StaticResource AppBg}" WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI Variable, Segoe UI" TextOptions.TextRenderingMode="ClearType">

    <Grid>
        <!-- ================= CONNECT PANEL ================= -->
        <Grid x:Name="ConnectPanel">
            <!-- soft accent glow in the corner -->
            <Ellipse Width="520" Height="520" HorizontalAlignment="Right" VerticalAlignment="Top"
                     Margin="0,-180,-160,0" Opacity="0.20" IsHitTestVisible="False">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#2F7CF6" Offset="0"/>
                        <GradientStop Color="#002F7CF6" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,10">
                <StackPanel Width="470" Margin="0,28,0,28">

                    <!-- brand -->
                    <StackPanel Orientation="Horizontal" Margin="2,0,0,22">
                        <Border Width="42" Height="42" CornerRadius="12" Background="{StaticResource AccentGrad}">
                            <TextBlock Text="◆" FontSize="20" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="LiteRemote" FontSize="24" FontWeight="Bold"/>
                            <TextBlock Text="Fast · lightweight · secure remote desktop"
                                       Foreground="{StaticResource Muted}" FontSize="12.5"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- main card -->
                    <Border Background="{StaticResource Panel}" CornerRadius="18" Padding="22"
                            BorderBrush="{StaticResource Stroke}" BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.35"/>
                        </Border.Effect>
                        <StackPanel>

                            <!-- mode toggle -->
                            <Border Background="{StaticResource Bg}" CornerRadius="10" Padding="4"
                                    BorderBrush="{StaticResource Stroke}" BorderThickness="1">
                                <UniformGrid Rows="1" Columns="2">
                                    <RadioButton x:Name="ModeAddrRadio" GroupName="mode" Style="{StaticResource Segment}"
                                                 Content="By address" IsChecked="True" Checked="Mode_Changed"/>
                                    <RadioButton x:Name="ModeIdRadio" GroupName="mode" Style="{StaticResource Segment}"
                                                 Content="Connect by ID" Checked="Mode_Changed"/>
                                </UniformGrid>
                            </Border>

                            <!-- ID mode -->
                            <StackPanel x:Name="IdModePanel" Margin="0,18,0,0" Visibility="Collapsed">
                                <TextBlock Text="PARTNER ID" Style="{StaticResource Label}"/>
                                <TextBox x:Name="IdBox" FontSize="22" FontWeight="SemiBold"
                                         Padding="12,10" Text=""/>
                                <TextBlock Text="Enter the 9-digit ID shown on the remote computer."
                                           Foreground="{StaticResource Muted}" FontSize="11.5" Margin="2,6,0,0"/>
                            </StackPanel>

                            <!-- address mode -->
                            <Grid x:Name="AddrModePanel" Margin="0,18,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="HOST ADDRESS" Style="{StaticResource Label}"/>
                                    <TextBox x:Name="HostBox" Text="192.168.1.10"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="PORT" Style="{StaticResource Label}"/>
                                    <TextBox x:Name="PortBox" Text="7443"/>
                                </StackPanel>
                            </Grid>

                            <!-- credentials -->
                            <TextBlock Text="SIGN IN WITH" Style="{StaticResource Label}" Margin="2,18,0,5"/>
                            <Border Background="{StaticResource Bg}" CornerRadius="10" Padding="4"
                                    BorderBrush="{StaticResource Stroke}" BorderThickness="1">
                                <UniformGrid Rows="1" Columns="2">
                                    <RadioButton x:Name="AuthPasswordRadio" GroupName="auth" Style="{StaticResource Segment}"
                                                 Content="Password" IsChecked="True" Checked="AuthMethod_Changed"/>
                                    <RadioButton x:Name="AuthGoogleRadio" GroupName="auth" Style="{StaticResource Segment}"
                                                 Content="Google account" Checked="AuthMethod_Changed"/>
                                </UniformGrid>
                            </Border>

                            <StackPanel x:Name="PasswordArea" Margin="0,14,0,0">
                                <TextBlock Text="HOST ACCESS PASSWORD" Style="{StaticResource Label}"/>
                                <PasswordBox x:Name="PasswordBox"/>
                                <TextBlock Foreground="{StaticResource Muted}" FontSize="11.5" Margin="2,6,0,0" TextWrapping="Wrap"
                                           Text="The password created on the remote computer: LiteRemote Host tray icon → “Set access password”."/>
                            </StackPanel>
                            <StackPanel x:Name="GoogleArea" Margin="0,12,0,0" Visibility="Collapsed">
                                <TextBlock Text="GOOGLE OAUTH CLIENT ID" Style="{StaticResource Label}"/>
                                <TextBox x:Name="GoogleClientIdBox"/>
                                <TextBlock Text="A browser window will open to sign you in."
                                           Foreground="{StaticResource Muted}" FontSize="11.5" Margin="2,6,0,0"/>
                            </StackPanel>

                            <!-- connect -->
                            <Button x:Name="ConnectButton" Content="Connect" FontSize="15"
                                    Padding="16,12" Margin="0,20,0,0" Click="Connect_Click"/>
                            <TextBlock x:Name="StatusText" Foreground="{StaticResource Muted}"
                                       TextWrapping="Wrap" Margin="2,10,0,0" MinHeight="16"/>
                        </StackPanel>
                    </Border>

                    <!-- advanced -->
                    <Expander x:Name="AdvancedExpander" Header="Advanced — performance, display, VPN &amp; relay"
                              Foreground="{StaticResource Muted}" Margin="4,16,0,0">
                        <Border Background="{StaticResource Panel}" CornerRadius="16" Padding="20" Margin="0,10,0,0"
                                BorderBrush="{StaticResource Stroke}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="RELAY SERVER (for ID connections)" Style="{StaticResource Label}"/>
                                <TextBox x:Name="RelayBox" Text=""/>
                                <TextBlock Text="e.g. relay.example.com:7500 — same server the host registers with."
                                           Foreground="{StaticResource Muted}" FontSize="11" Margin="2,5,0,0"/>

                                <!-- FPS & resolution live in the session toolbar only (applied live),
                                     so there is exactly one place to control them. -->
                                <TextBlock Text="IMAGE QUALITY" Style="{StaticResource Label}" Margin="2,16,0,5"/>
                                <StackPanel Orientation="Horizontal">
                                    <Slider x:Name="QualitySlider" Width="260" Minimum="20" Maximum="95" Value="75"
                                            VerticalAlignment="Center" ValueChanged="Quality_Changed"/>
                                    <TextBlock x:Name="QualityLabel" Text="75" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                </StackPanel>

                                <TextBlock Text="NETWORK PATH" Style="{StaticResource Label}" Margin="2,16,0,5"/>
                                <ComboBox x:Name="NetworkModeBox" SelectedIndex="0" SelectionChanged="NetworkMode_Changed">
                                    <ComboBoxItem>Direct internet / LAN</ComboBoxItem>
                                    <ComboBoxItem>Through VPN profile (this app only)</ComboBoxItem>
                                </ComboBox>
                                <StackPanel x:Name="VpnArea" Orientation="Horizontal" Margin="0,8,0,0" Visibility="Collapsed">
                                    <TextBox x:Name="VpnProfileBox" Width="300" IsReadOnly="True" Text="(no .ovpn profile selected)"/>
                                    <Button Content="Browse…" Style="{StaticResource Ghost}" Margin="8,0,0,0" Click="BrowseVpn_Click"/>
                                </StackPanel>

                                <WrapPanel Margin="0,16,0,0">
                                    <CheckBox x:Name="ClipboardCheck" Content="Sync clipboard" IsChecked="True" Margin="0,0,18,8"/>
                                    <CheckBox x:Name="BlankScreenCheck" Content="Blank host screen" Margin="0,0,18,8"/>
                                    <CheckBox x:Name="LockInputCheck" Content="Lock host keyboard/mouse" Margin="0,0,0,8"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- ================= SESSION PANEL ================= -->
        <Grid x:Name="SessionPanel" Visibility="Collapsed" Background="#05060A">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border x:Name="SessionToolbar" Grid.Row="0" Background="{StaticResource Panel}" BorderBrush="{StaticResource Stroke}"
                    BorderThickness="0,0,0,1" Padding="12,8">
                <DockPanel LastChildFill="False">
                    <Border DockPanel.Dock="Left" Width="9" Height="9" CornerRadius="5"
                            Background="{StaticResource Good}" VerticalAlignment="Center" Margin="2,0,10,0"/>
                    <TextBlock x:Name="MonitorLabel" DockPanel.Dock="Left" Text="Monitor" Foreground="{StaticResource Muted}"
                               VerticalAlignment="Center" Margin="0,0,6,0"
                               ToolTip="Layar fisik di PC host yang ditampilkan (bila host punya beberapa monitor). Angka = resolusi asli monitor itu."/>
                    <ComboBox x:Name="DisplayBox" Width="150" DockPanel.Dock="Left" SelectionChanged="Display_Changed"
                              ToolTip="Pilih monitor host yang ingin dilihat."/>

                    <TextBlock DockPanel.Dock="Left" Text="FPS" Foreground="{StaticResource Muted}"
                               VerticalAlignment="Center" Margin="16,0,6,0"/>
                    <ComboBox x:Name="SessionFpsBox" Width="100" DockPanel.Dock="Left" SelectedIndex="0"
                              SelectionChanged="SessionFps_Changed">
                        <ComboBoxItem>Auto</ComboBoxItem>
                        <ComboBoxItem>15</ComboBoxItem>
                        <ComboBoxItem>24</ComboBoxItem>
                        <ComboBoxItem>30</ComboBoxItem>
                        <ComboBoxItem>45</ComboBoxItem>
                        <ComboBoxItem>60</ComboBoxItem>
                        <ComboBoxItem>75</ComboBoxItem>
                        <ComboBoxItem>90</ComboBoxItem>
                        <ComboBoxItem>120</ComboBoxItem>
                        <ComboBoxItem>144</ComboBoxItem>
                    </ComboBox>

                    <TextBlock DockPanel.Dock="Left" Text="Res" Foreground="{StaticResource Muted}"
                               VerticalAlignment="Center" Margin="16,0,6,0"
                               ToolTip="Perkecil ukuran stream untuk hemat data / koneksi lambat. 'Default' = resolusi asli (paling tajam). Nilai lebih kecil = lebih ringan tapi kurang tajam."/>
                    <ComboBox x:Name="SessionResBox" Width="130" DockPanel.Dock="Left" SelectedIndex="0"
                              SelectionChanged="SessionRes_Changed"
                              ToolTip="Default = tajam (resolusi asli host). Pilih lebih kecil hanya bila koneksi lambat.">

                        <ComboBoxItem>Default</ComboBoxItem>
                        <ComboBoxItem>4K (3840×2160)</ComboBoxItem>
                        <ComboBoxItem>2560×1600</ComboBoxItem>
                        <ComboBoxItem>2560×1440</ComboBoxItem>
                        <ComboBoxItem>1920×1080</ComboBoxItem>
                        <ComboBoxItem>1680×1050</ComboBoxItem>
                        <ComboBoxItem>1366×768</ComboBoxItem>
                        <ComboBoxItem>1280×1024</ComboBoxItem>
                        <ComboBoxItem>1280×800</ComboBoxItem>
                        <ComboBoxItem>1280×720</ComboBoxItem>
                        <ComboBoxItem>1024×768</ComboBoxItem>
                    </ComboBox>

                    <TextBlock DockPanel.Dock="Left" Text="Codec" Foreground="{StaticResource Muted}"
                               VerticalAlignment="Center" Margin="16,0,6,0"/>
                    <ComboBox x:Name="SessionCodecBox" Width="150" DockPanel.Dock="Left" SelectedIndex="0"
                              SelectionChanged="SessionCodec_Changed"
                              ToolTip="Otomatis: H.264 hardware bila didukung (PC fisik), selain itu JPEG. JPEG: paling kompatibel.">
                        <ComboBoxItem>Otomatis</ComboBoxItem>
                        <ComboBoxItem>H.264 (hemat data)</ComboBoxItem>
                        <ComboBoxItem>JPEG (kompatibel)</ComboBoxItem>
                    </ComboBox>

                    <Button Content="Ctrl+Alt+Del" Style="{StaticResource Ghost}" DockPanel.Dock="Left" Margin="16,0,0,0" Click="Cad_Click"/>
                    <Button Content="Fullscreen" Style="{StaticResource Ghost}" DockPanel.Dock="Left" Margin="8,0,0,0" Click="Fullscreen_Click"/>

                    <Button Content="Disconnect" DockPanel.Dock="Right" Background="{StaticResource Bad}" Click="Disconnect_Click"/>
                    <TextBlock x:Name="StatText" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,16,0"
                               Foreground="{StaticResource Muted}" FontFamily="Cascadia Mono, Consolas" FontSize="12" Text="—"/>
                </DockPanel>
            </Border>

            <Grid Grid.Row="1" x:Name="SurfaceHost" Background="#05060A" ClipToBounds="True">
                <Image x:Name="RemoteImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"
                       SnapsToDevicePixels="True"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
